name: calorie_burn
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  provider: ^6.1.2
  health: ^9.0.0
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2 # 이 줄을 추가하세요
  camera: ^0.11.1+1 # 카메라 제어
  path_provider: ^2.1.3 # 파일 시스템 경로
  path: ^1.9.0 # 경로 결합
  http: ^1.2.2 # 외부 서버와 통신하기 위해 추가
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true